[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "greeceapt"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "Pillow",
    "requests",
    "curl-cffi",
    "playwright",
]

[tool.setuptools.packages.find]
where = ["src"]
